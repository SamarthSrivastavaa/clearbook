# --- secrets: never commit ---
.env
.env.*
!.env.example
*.key
*.pem

# --- node ---
node_modules/
npm-debug.log*

# --- foundry (Phase 1+) ---
contracts/out/
contracts/cache/
contracts/broadcast/

# --- build output ---
dist/
.next/

# --- internal build specification: not published ---
# Scoped to this one file on purpose. A blanket `*.md` would also exclude
# README.md, DECISIONS.md, KNOWN_ISSUES.md, SECURITY.md and docs/*.md, which
# BUILD.md §16 requires the public repo to contain.
/BUILD.md
